# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js build & cache
/.next/
/out/
/build
.turbo
.swc

# Vercel
.vercel

# Environment variables (NEVER commit secrets)
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local
# Keep example env for setup docs
!.env.example

# Local Turso / SQLite (dev only — production uses Turso cloud)
local.db
local.db-*
*.db
*.db-journal
*.db-shm
*.db-wal

# Drizzle (optional local migration artifacts)
drizzle/*.sql.backup

# TypeScript
*.tsbuildinfo

# Testing
/coverage
.nyc_output

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors / IDEs
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.swp
*.swo
*~

# Certificates / keys
*.pem
*.key
*.p12

# Uploaded PDFs (if ever stored locally during dev)
/uploads/
/tmp/
